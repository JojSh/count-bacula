The latest iteration of Count BACula is deployed at https://count-bacular.nw.r.appspot.com/

To run locally you can build with:

### `npm run build`

To start:

### `npm start`

To test:

### `npm test`